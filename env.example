# IPMTTI Notification System Environment Variables
# Copy this file to .env.local and fill in your actual values

# ===========================================
# SITE URL (Recommended)
# ===========================================
# Used for canonical URLs and share images
VITE_SITE_URL=https://ipmtti.co.ke

# ===========================================
# EMAIL CONFIGURATION (Required)
# ===========================================
# SMTP settings for sending emails with PDF attachments
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@gmail.com
# IMPORTANT: Use the 16-character Gmail App Password WITHOUT spaces
SMTP_PASS=your-gmail-app-password
MAIL_FROM=your-email@gmail.com

# ===========================================
# WHATSAPP CLOUD API CONFIGURATION (Required)
# ===========================================
# WhatsApp Business API credentials from Meta for Developers
WHATSAPP_ACCESS_TOKEN=your-whatsapp-access-token
WHATSAPP_PHONE_ID=your-phone-number-id
WHATSAPP_TO=254715016300

# ===========================================
# FILE STORAGE CONFIGURATION (Optional)
# ===========================================
# Supabase configuration for storing PDFs for WhatsApp attachments
# If not configured, WhatsApp will only receive text messages
SUPABASE_URL=your-supabase-project-url
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
SUPABASE_BUCKET=your-storage-bucket-name

# ===========================================
# SETUP INSTRUCTIONS
# ===========================================

# 1. EMAIL SETUP (Gmail):
#    - Enable 2-Factor Authentication on your Gmail account
#    - Go to Google Account → Security → App passwords
#    - Generate a new app password for "Mail"
#    - Use this app password as SMTP_PASS (not your regular password)
#    - Paste it without spaces (Google shows it grouped, remove spaces)

# 2. WHATSAPP SETUP:
#    - Create a Meta for Developers account (developers.facebook.com)
#    - Create a new WhatsApp Business App
#    - Get your Access Token from the app dashboard
#    - Get your Phone Number ID from the app dashboard
#    - The phone number should be in format: 254715016300 (without +)

# 3. SUPABASE SETUP (Optional):
#    - Create a Supabase project
#    - Go to Storage and create a new bucket
#    - Get your project URL and service role key
#    - This enables PDF attachments in WhatsApp messages

# ===========================================
# TESTING
# ===========================================
# Run the test script to verify your configuration:
# npm run test:notifications
